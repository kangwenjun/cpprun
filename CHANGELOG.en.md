# Changelog

All notable changes to this repository will be documented in this file.

## 2026-04-01

- tests(utils): use hex lookup table in `hex_to_bits` to improve performance (commit e89ce0d)
- misc: added `RULES.md`, added `tests/utils/bits_to_hex.hpp` and `tests/utils/stopwatch.hpp`, removed `tests/bin/bin2hex.cpp`, added `CHANGELOG.en.md` (commit f3ae91b).
