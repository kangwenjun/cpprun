# 更新日志

记录本仓库的重要更改。

## 2026-04-01

- tests(utils)：在 `hex_to_bits` 中使用十六进制查找表以提高性能（提交 e89ce0d）
- 杂项：新增 `RULES.md`，新增 `tests/utils/bits_to_hex.hpp` 与 `tests/utils/stopwatch.hpp`，移除 `tests/bin/bin2hex.cpp`，新增 `CHANGELOG.en.md`（提交 f3ae91b）。
